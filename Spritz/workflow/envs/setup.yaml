# setup.yaml: full environment for spritz setup before uploading to protected server
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - snakemake-minimal =5.27.4
  - python =3.8
  - dotnet-sdk =5.0
  - unzip
  - wget
  - curl
  - git
  - sra-tools
